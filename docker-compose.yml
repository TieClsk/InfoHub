services:
  app:
    build: .
    ports:
      - "3000:3000"
    environment:
      - DATABASE_URL=${DATABASE_URL}
      - RAW_DATABASE_URL=${RAW_DATABASE_URL}
      - DEEPSEEK_API_KEY=${DEEPSEEK_API_KEY}
      - DEEPSEEK_BASE_URL=${DEEPSEEK_BASE_URL}
      - NEWS_API_KEY=${NEWS_API_KEY}
      - TIANXING_API_KEY=${TIANXING_API_KEY}
      - CRON_SECRET=${CRON_SECRET}
      - RAW_RETENTION_DAYS=${RAW_RETENTION_DAYS:-14}
    restart: unless-stopped

  # 可选：本地 PostgreSQL（如不使用 Prisma Dev）
  # postgres:
  #   image: postgres:16-alpine
  #   environment:
  #     POSTGRES_USER: infohub
  #     POSTGRES_PASSWORD: infohub
  #     POSTGRES_DB: infohub_main
  #   ports:
  #     - "5432:5432"
  #   volumes:
  #     - pgdata:/var/lib/postgresql/data
  #
  # postgres_raw:
  #   image: postgres:16-alpine
  #   environment:
  #     POSTGRES_USER: infohub
  #     POSTGRES_PASSWORD: infohub
  #     POSTGRES_DB: infohub_raw
  #   ports:
  #     - "5433:5432"

# volumes:
#   pgdata:
